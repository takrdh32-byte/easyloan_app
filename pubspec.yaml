name: EasyLoan
description: Emergency loan app
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.24.0
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  firebase_storage: ^11.5.0
  firebase_messaging: ^14.7.0

  permission_handler: ^11.0.0
  camera: ^0.10.5
  google_ml_kit: ^0.16.0

  path_provider: ^2.1.0
  pdf: ^3.10.0
  printing: ^5.11.0

  razorpay_flutter: ^1.3.6

  provider: ^6.0.5

  intl: ^0.18.1
  image_picker: ^1.0.4
  cached_network_image: ^3.3.0

  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
